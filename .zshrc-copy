
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/Users/hiro/miniconda3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/Users/hiro/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/Users/hiro/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/Users/hiro/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<

# powerline-shellの設定
function powerline_precmd() {
  PS1="$(powerline-shell --shell zsh $?)"
}

function install_powerline_precmd() {
  for s in "${precmd_functions[@]}"; do
    if [ "$s" = "powerline_precmd" ]; then
      return
    fi
  done
  precmd_functions+=(powerline_precmd)
}

if [ "$TERM" != "linux" ]; then
  install_powerline_precmd
fi

# ハイライト設定の追加
source /Users/hiro/Documents/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

# 初回シェル時のみ
if [ $SHLVL = 1 ]; then
    #tmux 現在無効
    #デフォルトブラウザをw3mに設定
    export BROWSER='w3m'
fi

# neofetchを使って端末情報を取得してアスキーアート表示
#neofetch

# homebrew completions in zsh
if type brew &>/dev/null
then
  FPATH="$(brew --prefix)/share/zsh/site-functions:${FPATH}"

  autoload -Uz compinit
  compinit
fi
# node バージョン管理システム fnmのpathを通す
eval "$(fnm env --use-on-cd)"

#デフォルトブラウザをw3mに設定
export BROWSER='w3m'
